mod find;
mod load_sigs;
mod scrape_four_bytes;

fn main() {
    // scrape_four_bytes::scrape_four_bytes().unwrap();
    // let sigs = load_sigs::load_sigs().unwrap();
    // let map = load_sigs::fill_hashmap(sigs);
}
